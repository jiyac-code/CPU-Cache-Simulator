##README

#CPU-Cache Simulator

#Overview
This is a CPU scheduler and memory cache simulator which simulates how tasks get allocated to a CPU and how memory blocks move through a cache hierarchy. It makes use of Round Robin scheduling algorithm.

#Folder Structure

